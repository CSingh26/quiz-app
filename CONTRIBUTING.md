# Contributing

Keep grading and validation in the domain layer, enforce roles on mutation routes and preserve the documented answer-text protocol. Reproduce behavior changes with meaningful tests. Run backend lint/tests, frontend lint/typecheck/build, browser journeys and the isolated MongoDB integration test before a pull request. Never commit `.env`, user data or storage credentials.
