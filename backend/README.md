# QuizBee API

Express/Prisma service for validated question imports, room lifecycle and server-side grading. Read the [root setup](../README.md), [architecture](../docs/ARCHITECTURE.md) and [methodology](../docs/METHODOLOGY.md). MongoDB requires a replica set. Environment variable names are in `.env.example`; actual values stay local.
