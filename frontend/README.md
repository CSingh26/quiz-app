# QuizBee interface

Student and instructor Next.js workflows. Read the [root setup](../README.md) and [testing guide](../docs/TESTING.md). `NEXT_PUBLIC_API_BASE_URL` is embedded during build and requires a trailing slash. Browser tests use labeled fixtures and do not submit real student records.
